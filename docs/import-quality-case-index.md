# 模型质量案例索引与关闭验收

本索引对应 [跟踪已登记的模型语义误判与标题译义问题（#47）](https://github.com/wooveep/UrltraKB/issues/47)
和 [改进问答中的冗余表达和技术性附加说明（#48）](https://github.com/wooveep/UrltraKB/issues/48)。
它完成逐例归档与现行验收，不宣称所有模型错误和冗余消失。2026-09-15 已接受的边界
继续有效：可靠资料正常发布、原件可读、缺失可见；保留关键事实/来源/人工编辑保护，
允许拒答、明确遗漏和已登记模型限制，由用户手动调整。

## 逐例对应

公开表只列脱敏复核点。每个ID在私有 `closure/cases.json` 中绑定确切原句/邻接上下文、
候选、预期与实际结果、运行ID、全部物理回执ID及文件哈希。完整原件与请求留在验证库；
历史页面已复制保留，后续手工编辑不会改写当时的观察。诊断、已发布知识、完成答案和
未完成草稿分列。表中的“首回执”用于定位，并不代表该案例只有一次调用。

| ID | 最小现象 | 复核点 | 结果类型、处置与边界 | 首回执 |
|---|---|---|---|---|
| M01 | 规则例外被标题译为故障 | 完整原句及相邻禁令未描述故障；候选标题增加“诊断异常” | 未部署分项诊断仍漏检；保留为模型限制，手工改为中性标题或原词 | `00620` |
| M02 | 中性标题误拒 | 候选仅称“诊断记录”，没有增加故障断言 | 未部署兼容格式候选误拒；不采用该候选，作为保护正例保留 | `00617` |
| M03 | 保留原词正例 | 保留 Exception，不强制新增中文技术解释 | 诊断得到肯定；仅为该请求组合的正例，不能证明所有标题核验正确 | `00622` |
| M04 | 原文明示失败正例 | 相邻原句明确禁止认证失败后的自动重启 | 诊断得到肯定；不按“失败”等关键词一律禁用标题 | `00623` |
| M05 | 模式条件改为时间频率 | night mode 是条件，nightly 改成频率 | 历史已发布知识中的限制；保留原词/“夜间模式”及批次条件，由用户编辑 | `00181` |
| M06 | 局部规则泛化为全部用途 | 某批次在特定模式下使用容器，不等于该容器所有用途仅限此规则 | 历史未完成回答；保留有界拒绝，删除无依据的全称附加句即可避免该断言 | `00303` |
| M07 | 无引用的附加缺失说明 | 表格字面查询后追加“未给出任何含义/用途/触发说明” | 历史完成答案的限制；该句未提供可核对引用，不据此断言它已被证伪；可删除或补充限定范围与来源 | `00278` |
| M08 | 明确相邻指代被过度质疑 | 绿卡仅邀请指定声部，紧邻下一句要求其他声部静默 | 历史完成答案仍作过强不确定性说明；保留限制，不能拿另一小文本通过冒充本例稳定修复 | `00559` |
| M09 | 明确指代与竞争指代对照 | 明确的组别规则须保留；可能指向两个人的代词须保留歧义 | 正常导入后问答完成；这是通用问答模板实际修改的正反行为证据 | `00684` |
| M10 | 附加范围和并列译义被拒 | 活动表回答新增页面范围概括；纠正后给 returning singers 增加“返场/老歌手”并列译义 | 正式回答未完成，未回滚来源；保留核验结果及模型判断局限，不把核验拒绝自动等同于人工证伪 | `00648` |
| M11 | 卡片问题独立覆盖 | 迟到等待、红卡全停、绿卡指定声部及其他声部静默均须保留 | 新增正式回答完成；仍有坐标/原生角色说明和不必要的指代疑虑，归入已有残留 | `00690` |
| P01 | 表格重复减少 | 两行、数值和排他条件在表中就地引用 | 正式完成，无第二份逐行清单；表头/邻接材料仍偏多 | `00636` |
| P02 | 原图说明减少 | 请求原图保留同一原始资产和来源 | 正式完成，去掉逐项帧/OCR字段；仍有多余的原图/OCR对比 | `00642` |
| P03 | 显式技术细节正例 | 明确要求原图、渲染帧、OCR输出角色及绑定 | 正式完成，三种角色和三个匹配哈希的资产均保留 | `00660` |
| P04 | 显式双语正例 | 明确要求原词与译文，排他条件不丢失 | 正式完成；原词、译文、数值及only均保留，重复复述仍登记 | `00672` |

用户手动调整时应保留原句、条件和来源引用；标题可保留原词或采用中性表达，额外范围
断言可直接删除。误拒造成的知识遗漏可从原件补写。手动编辑不自动把OCR/分析覆盖改为
完整，也不改变历史回执或自动质量成绩。

## 可运行回放与完整性检查

私有目录：`examples/kb/Ocloudware/outputs/issue45-p5-live-r4-2026-09-15/closure/`。
从仓库根目录运行：

```bash
.venv/bin/python examples/kb/Ocloudware/outputs/issue45-p5-live-r4-2026-09-15/closure/replay.py
```

结果：15个案例、335份冻结证据文件完整；声明的实际判定/状态与原回执一致，失败草稿
不能伪装成完整回合。脚本不调用模型、不写知识库，也不推断语义已正确。

增加 `--strict-model` 时退出码仍为1，明确报告M01漏检和M02误拒；这是历史模型不一致，
不是新的执行失败，更不能把“接受限制”改写成严格语义通过。篡改文件哈希和篡改已声明
判定的两个负向审计均被拒绝；原清单保持不变。

清单SHA-256：`f32c7968d0901f28f6f4341c9f4f942b20d6a3414257a2d97da4e6549285b37d`。
回放脚本SHA-256：`015dd841fd0ace6489c6240b6657dc4c482192a8c71c0e833ae72605ccdd1b76`。

## 按实际验收逐项核对

| Issue验收项 | 对应证据 | 结论 |
|---|---|---|
| #47 已登记现象有最小例、结果类型和处理结论 | M01–M11及私有完整回执，修复/正例/接受限制分列 | 满足 |
| #47 实际通用改动有正反行为验证，保护不放宽 | M09明确指代与竞争指代；既有54项发布/恢复/人工编辑和96项问答/来源回归 | 满足 |
| #47 成败及全部成本保留 | 严格回放保留不一致，账本包含诊断、失败、纠正与新增问答 | 满足 |
| #48 重复/无关技术说明减少且条件来源保持 | P01/P02正式入口，相同Word原件与问题；引用及原件哈希核对 | 满足，减少不等于全部消除 |
| #48 用户要求的细节仍可提供 | P03/P04技术来源与双语对照；无固定句数裁剪 | 满足 |
| #48 语义残留登记，问答不回滚发布 | M01–M11，M06/M10失败草稿保留，原件和已发布资料可读 | 满足 |

此前“尚未消除所有冗余/误判，因此继续开放”的表述比现行验收更严格。本次补齐逐例
归档后，不再将它当作隐藏的关闭条件，也不新建同名票来转移这些已接受限制。

## 读视图评估

本次未改变读取契约。原文上下文、资产角色、位置、覆盖与处理状态分别承担事实解释、
图示绑定和缺失可见性；既有诊断没有证明字段投影可以减少冗余且不损害这些作用。
通用问答模板已取得可复核的局部改善，显式细节请求也保留。因此不为关闭强行删除
字段、新增模式或隐藏必要证据。读视图改造不是两票现行验收的必需实现。

## 新增验证与账目

本次只补一个独立卡片问题，沿用`598aae1`的运行时和原小PPTX；它覆盖此前被跳过的
问题，不替换失败的活动表首问，也不计作原会话追问。该问题正式完成，主规则保留，
仍有技术说明和指代过度谨慎（M11），全部引用可读、原件哈希匹配。

新增 **12次物理请求、209,620已知tokens、未知0、OCR0**。R4累计 **701请求、7,167,165
已知tokens、未知0**；OCR仍为3页次POST/19 HTTP。137个实际操作、135份结果，正常
完成问答32份；诊断不计入完成回合。R1–R4合计 **1,761请求、16,158,177已知tokens**，
其中R1三次请求用量未知，金额未知。原6小时时限、额度和更早账本不重置。

本次没有修改运行时代码，文件哈希与上一轮完整回归时一致；沿用2,287 passed、1 skipped、
3 warnings及独立桌面I/O 1 passed的证据，不把它写成本次又跑了一遍。mypy315、改动文件
Ruff及双轴复核记录见 [上轮修复与验证](import-quality-followup.md)。本次Standards与Spec双轴复核均无剩余发现，确认两票满足现行验收。
本次新增验证工具的正/负审计结果在`closure/audit-results.json`，卡片结果在`issue48-followup/closure-card-audit.json`。
