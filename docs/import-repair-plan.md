# 导入修复与最小闭环计划

2026-09-15 第四轮进展：P0/P1 已交付，#13/#25/#46 保持关闭。DOCX 诊断分离、核验
数字/路径、编译译义、图片关联与原生资产来源、PPTX原生角色、视觉说明范围及审核冲突诊断
已完成十批通用修复；小文件继续证明内容遗漏及零知识正常发布。独立样本仍有附加说明、
误译及过强不确定性；已修复协议问题的回归有改善，#45/#34/#23 保持开放。
维护者已取消成本关闭门槛，费用继续全部列报。
发布完成不代表语义和等价对照全部通过，剩余问题及执行顺序见文末最新计划。
本文件保留此前验收约定和历史记录；新增授权与旧轮分账，最新证据见
[第四轮验证报告](import-p5-live-r4-validation.md)，历史见
[第三轮验证报告](import-minimal-r3-validation.md)、
[第二轮记录](import-minimal-r2-validation.md) 和 [第一轮记录](import-minimal-validation.md)。

## 最新验收修订：成本完整列报，不作为关闭门槛

2026-09-14，维护者明确选择：“成本全部列报，但不再作为关闭门槛”。
本修订优先于本计划和相关 spec/issue 历史记录中的成本门槛条款。

- 取消逐例历史累计 tokens/B0≤1.10 及成本超限对 issue 关闭的阻塞；不再等待成本口径确认。
- 历史开发、失败、重试、恢复、基线和新运行费用全部保留；请求、tokens、可取得的金额、
  OCR 页次和耗时如实列报。未知用量或金额明确标未知，不补零、不据此声称节省。
- B0/B1 身份和原始账本不变；导入/问答、冷运行/复导/恢复继续分列。只有同能力、
  同覆盖、同质量的有效对照能支持性能收益结论；费用高低本身不再决定关闭状态。
- 每轮已授权的 tokens、请求、时间和 OCR 页次仍是执行硬上限。取消关闭门槛不授予
  新额度、不重开已封存实验，也不改变正常发布、内容遗漏、语义核验及人工编辑保护。

按此口径复核，#45 的成本阻塞已解除；其知识注解污染、额外回答断言及独立质量结果
仍未满足。#34 的功能/覆盖等价对照仍缺，#23 依赖两者；三票保持开放。
#13/#25/#46 保持关闭。

## 首要原则

> 允许文档导入存在异常，但不要因此中断、中止或退出正常发布。
> 内容可以缺失，用户后续可以手动编辑知识调整。验证使用小文件完成最小闭环。

普通解析、OCR、附件、提取、生成及核验问题，在有限恢复后作为内容缺失处理。
已有可靠内容正常发布，其他文件继续；用户不必先修复或逐项确认缺失。
错误候选可以排除，但其失败不能成为其他知识发布的前置门槛。
缺失前提会使某段操作变义时，一起排除该段操作，继续独立内容。

本次进一步明确边界：原件已可靠保存，但本次没有可生成知识时，也应完成资料登记，
提供可编辑的来源/缺失说明入口，显示“导入完成，有内容缺失；生成知识 0 条”。
不生成虚假的知识页，不把 0 条知识或 OCR 转写称作完整分析。
P0 已取消全候选排除时提前返回未完成的门槛，零知识与部分知识走相同发布事务。

原件、版本、引用历史和其他来源贡献保留；用户补写内容沿用既有人工编辑保护。
普通复导不覆盖人工内容、不重新抽取已完成部分；显式继续处理缺口及受影响依赖。
只有用户停止、硬额度、身份/存储/事务无法保证等执行约束才允许停止相应执行。
远端状态不明的工作不得盲目重发，其本地缺失状态与其他独立内容的发布分别处理。

发布结果与覆盖分开：正常提交可以带 `omissions`、警告及 `coverage=partial`；
旧记录无法核对时保持 unknown。问答未通过不能回滚已导入资料，
用户手工补写也不能自动将原件 OCR 或分析覆盖改成 complete。

## Issue 核对结论（2026-09-13 历史状态）

共核对 46 个 issue 的状态；40 个已经关闭，6 个仍开放。
完成的实现切片保持关闭，不因扩大验证要求而重做。
本次没有新增满足全部关闭条件的开放 issue，不以“代码已有”替代剩余验收。

| Issue | 已有成果 | 尚不能关闭的原因 | 关闭条件 |
|---|---|---|---|
| [#46](https://github.com/wooveep/UrltraKB/issues/46) | 覆盖清单、局部隔离、共享复用、分层核验、回答校验、OCR 续跑已交付 | P0/P1 已实现，组合回归与双向复核通过，已满足关闭条件 | P0-1、P0-2 通过；P1 的通用回归落地，不要求所有大文档或模型零错误 |
| [#13](https://github.com/wooveep/UrltraKB/issues/13) | 三批主流程、平台/OCR 历史验证、真实四格式发布 | 新发布边界已贯通；仍缺本轮小 PDF/DOCX 云 OCR 正式采纳闭环 | P0 与 P2 的小 PDF/DOCX 闭环通过；不以大文件全量 OCR 为门槛 |
| [#25](https://github.com/wooveep/UrltraKB/issues/25) | 阶段、请求和执行链计量已有实现 | 缺同一冻结小样本的完整冷 B0，开发续跑不能代替 | P3 的 B0、复导及恢复账本完整，未知费用如实报告 |
| [#34](https://github.com/wooveep/UrltraKB/issues/34) | 数据库索引及原文回读已贯通 | 旧 B1 使用旧索引或不同覆盖；缺当前能力的有效等价对照 | P3 冻结数据库 B1 与候选，逐例核对能力、保留内容和缺失集合 |
| [#45](https://github.com/wooveep/UrltraKB/issues/45) | 23 份离线接入历史证据；四格式真实发布和零请求复导 | 机械引用与修正范围已修复并受控验证；真实效果及可比成本仍待验证 | P1、P2、P3 的固定小样本结果及逐例结论齐全；失败不换样本冲掉 |
| [#23](https://github.com/wooveep/UrltraKB/issues/23) | 多个实现子票已完成，数据库路线已有代码 | 汇总验收依赖以上缺口 | 子项门槛完成后逐项复核；不能仅因开发切片完成关闭总规格 |

最近完整回归为 2,100 passed、1 skipped（desktop_io 独立进程），mypy 303 文件通过；
Standards/Spec 复核无可操作问题。这是既有实现证据，不证明本次新增发布边界已实现。
最近真实问答的端口案例和 SNMP 案例均未通过，未保存为完成答案。
累计开发模型请求 1,338 次、31,758,399 tokens，云 OCR 保守计 28 页次提交；失败成本保留。

## 执行顺序与具体交付

### P0-1：让内容耗尽走正常登记与发布收尾（#46）

- 在现有事实、规划、生成、依赖检查和文档应用流程中统一普通内容遗漏结果。
  将“所有候选被排除”与身份、事务、用户停止、全局预算故障分开，不能笼统捕获后者。
- 删除依赖“必须有一个成功主题”才能登记来源的内容门槛。没有知识时，事务仅提交
  来源入口、缺失说明和发布登记；有知识时同一事务提交可靠知识及缺失说明。
- 更新旧版本时不得把旧知识假装成本次新内容；按来源贡献规则撤回失效自动内容，
  历史引用仍可读，其他来源与人工补写继续受保护。
- 主要范围：`agent/evidence_compiler.py`、`evidence_facts.py`、`evidence_dependencies.py`、
  `application/document_pipeline.py`、`knowledge_commit.py` 和结果状态映射。
- **最小验收**：两段独立内容，一段失败另一段发布；全部提取/生成/依赖候选被排除时
  导入完成、知识为 0、缺失可见、来源可打开；随后同批正常文件仍完成。
  坏原文、未知模型返回不被编造成事实；旧版及人工内容保护仍通过。

### P0-2：缺失后的编辑、复导与继续形成闭环（#46、#13）

- 复用现有知识编辑入口；从导入结果打开已发布知识或零知识资料入口，查看缺失原因。
  用户补写不需要先执行 OCR 或再次导入，缺失诊断不作为编辑阻塞条件。
- 普通同版本复导复用已完成登记，不再因缺失重新计费；显式 Continue 才处理有效缺口。
  用户补写与自动生成区分，后续继续/更新不得静默覆盖。
- 单页 OCR 错误、队列拒绝、传输中断、图片不可识别分别记为局部缺失。
  已受理作业沿原身份查询；执行状态不明时不重复 POST，其他独立内容继续。
- **最小验收**：1 张图加 2 段文字，图失败仍发布文字；编辑补写→普通复导→Continue，
  人工文字、旧引用保留，已受理 OCR 不重复提交。CLI 返回 0、REST 与桌面显示完成加缺失。
  用户停止与硬额度仍停止新请求，不能通过“容错”继续消费。

### P1：减少回答中的机械引用错误和修正副作用（#46 回归、#45 验收）

- 工具返回短证据标识，程序将其映射到完整来源链接并渲染路径和锚点。
  模型只选择本次实际观察的标识；未知或歧义标识不能模糊匹配、猜测修正为合法引用。
  兼容现有有效引用；不再要求模型反复手写长哈希路径。沿用现有工具和渲染入口。
- 引用存在和语义支持分别校验。表格按表头、同行条件核对；术语名称不补成用途/别名；
  未完整读取不能断言全文没有某项内容；图注明确位置才建立左右对应关系。
- 语义修正用可定位单元替换，程序保留未受影响单元和证据绑定；跨单元改动时
  明确受影响范围并重核。保留原文条件，检查章节引用；沿用有限修正次数和总预算。
  已实现受影响单元 ID、限定 JSON 修正与完整重核；后续引用修正同样受原范围约束。
- **最小验收**：6 行表含一条不同条件、两段条件说明和两幅带图注示意图；
  各一条问题加一条追问。无坏链接、无额外分类、无遗漏已读必要条件、图示不互换。
  受控 HTTP 服务覆盖错误返回；真实用例失败保留诊断，不重复判定直到通过。
  回答质量检查不进入导入发布的阻塞路径。

### P2：固定小文件完成真实导入与编辑闭环（#13、#45）

先冻结下面的脱敏小样本及原件锚点；每个文件只覆盖必要结构，避免大文件重复校准。

| 样本 | 上限与内容 | 观察重点 |
|---|---|---|
| Markdown/文本 | 2 KB，3 段含前提、否定和例外 | 部分/全部遗漏，独立内容继续，编辑保护 |
| PDF | 2 页，1 页原生文字、1 页扫描文字/带左右图注示意图 | 云 OCR、页码与图示对应，单页失败不阻断 |
| DOCX | 2 页，2 个标题、1 个小表、1 张内嵌图 | 原文位置、图像转写、缺失后的人工补写 |
| XLSX | 1 表，6 行数据加表头 | 条件不同的相邻行、完整枚举、无用途臆测 |
| PPTX | 2 页，文本和 2 个可定位对象 | 幻灯片顺序、对象绑定、原文回读 |

每种格式最初只做一次冷导入、一条问答和一条追问；另选一例做普通复导与一次 Continue。
纯故障组合用受控服务的小样本完成，不把所有故障都变成真实 API 实验。
云 OCR 的正常采纳和恢复最多用上述 PDF 与 DOCX 的 3 个单页输入；图片理解未启用时
保持缺失，不为关闭导入票强行补齐视觉识别。图示问答只使用有明确图注/位置的证据。

固定输入、预期保留与允许缺失的锚点、模型选项、初始状态和输出预算；首轮失败后
先修改通用代码或提示，再对该最小案例复测。不得更换样本或手改知识来抹掉失败。
在这五个文件内预先固定角色：文本、DOCX、XLSX 用于校准，PDF、PPTX 用于独立验证；
两组使用不同语义内容，同系列或同内容的格式副本不能跨组。独立组在代码与提示冻结后
再运行；一旦根据其失败调参，后续复测只记回归，不继续冒称独立验证。
没有有效独立结果时，#45 的对应条件保持未通过；不自动增加文件或付费运行。
正常人工编辑另列为功能步骤，不计入自动生成质量通过记录。
原有 23 份和长文记录留作历史兼容/压力证据，不再要求重新全量导入后才关闭这些票。
小文件通过不推广为长文吞吐、完整 OCR 或普遍模型准确率结论。

### P3：补齐可比的小样本基线（#25、#34、#45）

- B0 仍使用 #24 冻结的 `b4c7ce3` 及其实际依赖/配置快照，不能把最新实现重新命名为 B0。
  采用本次固定小文件新做完整冷运行；历史大文件续跑不替代。必要计量探针单列差异，
  不改变业务决策。某格式在 B0 无等价能力时标“不可比”，不补零或编造比率。
- 数据库 B1 以 `c04d8be` 为待核对起点，先验证对应快照能力；若含义/覆盖不同，
  不得直接认定为有效 B1。候选版在 P0/P1 完成后固定提交、依赖、配置与初始状态。
- 首轮每例只跑 B0、B1、候选各一次校准，记录索引、失败、修正、发布等全部费用；
  问答单列。同源复导、恢复、跨来源复用、变更版本分别记账。
- 比较对象可以均有允许缺失，但保留锚点、缺失集合与能力必须相等。
  缩减知识得到的节省不算同质量收益；历史开发支出保留，不用新小样本重置旧账本。
- 只有需要声明提速时，才对已通过质量门槛的小案例追加到三组成对观测，交错顺序，
  记录提供方缓存与模型实际版本。未有稳定收益就报告无结论，不扩充大文件刷结果。
- 按最新修订，#23/#45 不再以累计 tokens/B0≤1.10 或成本超限阻止关闭。
  全部费用和未知用量仍列报；不可比运行不能用于性能收益结论。优化重复输入和有效
  复用可继续进行，但不能降低知识质量，也无需为消除历史费用而追加实验。

## 完成定义与执行边界

顺序：P0-1 → P0-2 → P1 → P2 → P3；小样本与独立锚点可在 P0/P1 开发时准备。
P0/P1 的受控回归通过后复核 #46；P2 的 PDF/DOCX 闭环通过后复核 #13；
P3 分别满足 B0/B1 条件后复核 #25/#34，随后复核 #45，最后 #23。
遇到样例失败，只新增有依据的通用修复，不新增无限重试或文档特判。

每个切片先以现有应用入口复现，再修复并运行相应回归、类型/格式检查；最终组合运行完整回归。
CLI/REST/桌面只补少量状态和编辑契约，不重复三份语义测试。
P0/P1 开发使用受控外部服务，不启动新付费实验。真实验证前先读取有效授权和累计账本，
每个小闭环预留有限请求、tokens、OCR 页数与时间，不自动延长此前 10 小时授权。
公共 issue 只记录通用方案和汇总结果，原件、完整请求/答案与凭据继续留在指定验证库。


## 2026-09-14 P0/P1 交付记录（小文件真实验证前）

P0-1、P0-2、P1 已满足本计划的受控验收，#46 已按完成关闭并读回核实。
修复提交为 `bd14000`（本地，尚未推送）。目前开放票为 #13、#23、#25、#34、#45，
各票已写入剩余原因和 P2/P3 执行步骤。通用改动覆盖：

- 解析内容耗尽、全部事实/规划/生成/核验/依赖失败，都能正常登记并发布零知识摘要；
  原件、缺失诊断和 partial 覆盖保留。明确的坏 PDF、Office 包、包主关系和文本编码
  归入内容遗漏；全局预算、用户停止、输入/资产/事务完整性仍按原约束处理。
- 人工补写后，同候选 Continue 和复导不覆盖或重新认领人工文字。新的真实差异
  继续受人工编辑保护；归属基线及待接受提案中的旧引用经历史清理后仍保留。
- 引用由已观察短标识确定性渲染；问答、TTY 和保存历史使用完整链接。语义修正
  绑定核验确认的单元，重复词句不扩大范围，跨单元问题及后续引用修正保留范围。
  无效核验只重核原答案一次；修改后的全文仍通过独立核验。

验证：完整组合回归 **2,138 passed、1 skipped**；该运行期间复核发现的终端和修正
范围问题修复后，最终回答/终端专项 **106 passed**；桌面 I/O 独立进程 **1 passed**。
最终 mypy **309 个源文件通过**，本次修改的 Ruff 与 diff 空白检查通过。
测试使用共享工作树；本次提交只包含该修复的文件，未纳入其他已有工作。

Standards 复核：0 项剩余问题。Spec 复核：0 项剩余问题。
复核曾发现且已修复：仅图片的成功 VML 转写被遗漏、待接受引用可能被清理、Office
必要部件及非法主关系未分类、TTY 未展开短引用、重复词扩大修正范围、后续引用
修正绕过范围。相关反例均保留为小型受控回归。

真实 API 新增请求 **0**，新增云 OCR 页次 **0**。之前的真实失败与账本保持不变。
已准备 `examples/docs/issue46-minimal/` 中 5 个合成小文件：文本 249 B、DOCX 14,772 B、
XLSX 5,043 B、PDF 36,327 B、PPTX 29,388 B；文本/DOCX/XLSX 用于校准，内容不同的
PDF/PPTX 用于独立验证。PDF 两页（文字/扫描各一页）；DOCX 一个显式分页、两个标题、
一表一图；XLSX 六行加表头；PPTX 两页。已检查文件结构，但这不等于真实导入通过。

清单、原件哈希、固定语义锚点、问题/追问、B0/B1 依赖快照及本轮回归证据保存在：
`examples/kb/Ocloudware/outputs/issue46-repair-2026-09-14/`。
B0/B1 的能力等价、冷运行及成本均仍为未验证，不因有依赖快照就标为通过。

剩余顺序明确为 P2 → P3：有效新额度到位后，按冻结样本做最小真实导入/问答/OCR，
满足 PDF/DOCX 条件才关闭 #13；再补可比 B0/B1，分别复核 #25/#34，最后 #45/#23。
当前不关闭这五票，不以受控模型通过代替真实质量或成本结论。

## 2026-09-14 小文件轮修复与剩余执行计划

本轮授权为 5,000 万 tokens、100 次模型请求、4 小时、50 页云 OCR。物理模型请求
累计达到 100 次后已停止付费调用；已知 774,638 tokens，3 次用量未知，含未知预留
计 836,139。云 OCR 实际提交 2 个单页，均正式下载并进入正常解析回读。剩余 token
和 OCR 页数不能抵消请求上限；旧轮失败和支出保留，不因换小文件而清零。

通用修复已落地：回答修正允许省略空操作数组；Continue 保留已完成规划成员及有效
主题核验回执，新增无关主题不再使旧主题重生成，失效的实际引用仍触发重新生成；
单个主题超出请求容量时作为缺失排除，允许其余内容或零知识入口正常发布。
没有按文档名、品牌、固定内容或预期值修改生产逻辑，未手改知识以提高自动质量成绩。

| 优先级 / Issue | 当前未满足的具体原因 | 下一步及完成条件 |
|---|---|---|
| P2-1 / #13、#45 | DOCX 人工编辑与零请求复导已通过；真实 Continue 在旧冻结代码上重复规划，并被硬额度停止 | 先固定修复后的代码/配置，利用同一已保存来源和 OCR 身份完成一次 Continue，核对仅补缺口、人工字节及旧引用保留；不重传已受理作业 |
| P2-2 / #13、#45 | PDF OCR 正式采纳和原文回读已完成，但知识尚未发布；PDF/PPTX 尚无一问一追问 | 按原冻结语义不同的两份小文件，完成发布和问答闭环；最终修复版没有独立验证成绩，不以旧版部分解析代替 |
| P3-1 / #25 | 保存了 B0 源码/依赖，未完成同一小样本冷运行 | 使用 b4c7ce3，先核对支持能力与计量探针，再完成可比案例冷导入，复导/恢复单列；未知用量不补零 |
| P3-2 / #34 | 只读核对已发现 B1 的零知识发布、最终答案核验不同于候选；尚无行为及覆盖等价记录 | 保留 c04d8be 历史身份，按实际能力、保留锚点与缺失集合筛选可比案例，再进行同输入配对；不同边界单列，不偷偷补改基线 |
| P3-3 / #45 | 小表冷导入出现重复主题，且问答核验首轮输出被截断；无有效逐例成本比 | 优先检查表头、行值和行关系的语义合并及重复证据输入；用受控小表证明不同条件不丢失，再冻结候选。复用已有核验，导入/问答预算分别记录；仍须逐例累计 tokens/B0 ≤ 1.10 |
| 汇总 / #23 | 上述 P2/P3 门槛未齐 | 子项按实际完成关闭后复核总规格，不扩大为长文全量测试 |

#46 的完成状态保持不变。本轮没有新增达到全部关闭条件的开放 issue。
任何后续真实请求都需要新的有效额度；离线修复、结果整理和回归不受本轮请求上限影响。

本轮最终完整回归 **2,161 passed、1 skipped**，桌面 I/O 独立进程 **1 passed**；
mypy **309** 个源文件通过，Ruff 与 diff 检查通过；Standards/Spec 均无剩余问题。
全部 46 个 issue 已读回核对：41 个已关闭，5 个保持开放，最新原因与步骤已同步。

## 2026-09-14 第二轮交付与下一步

本轮授权为 8,000 万 tokens、2,000 次请求、5 小时、100 页次 OCR，覆盖 04:00:01–09:00:01 UTC。
依旧只使用冻结的五个小文件；正式返回、队列拒绝、错误回答、协议截断及所有恢复支出保留。

已交付 `699f5f3`、`094315f`、`5a6c14b`、`c09e20f`、`b47d7b3`：

- 云 OCR 二维位置不再丢失；严格验证未变换的栅格空间，提供显示方向坐标以对应明确左右图注。
  已下载结果可零 HTTP 重新装配；不把 OCR 字符转写当作图像理解。
- 问答核验得到实际能力开关；原文工具提供已绑定的 OCR 本地回执，明确队列拒绝不等于排队。
  损坏或无关联的旧账本只能报告未知，不挡正常原文读取。
- 核验要求逐字引用，允许保持原义的翻译，限制无关附加说明。
  提取、生成和核验区分组织标题与领域属性，检查小标题；PPTX 回归改善，PDF 仍有反例。
- 小 PDF/DOCX 正式 OCR、发布和 Continue 闭环已完成。DOCX 零知识发布后仍可通过正常编辑入口补写。
  人工编辑与历史引用保留；升级编译契约触发的实际覆盖差异仍进入保护提案，未擅自接受。
- B0 `b4c7ce3` 与 B1 `c04d8be` 均完成五文件原生空库冷导入、一问一追问、零请求普通复导；
  文本/PDF Continue 单列。B0/B1 的 PDF Continue 保留原 OCR 缺口，不冒称成功补全。

本轮 #13 的小文件导入关闭条件、#25 的冷 B0 交付条件已满足，最终测试通过后已关闭并读回。
#34、#45、#23 的剩余工作如下，不能因通用代码提交就判全部满足。

| 顺序 / Issue | 已确认的未满足原因 | 下一步修复与验证 | 完成条件 |
|---|---|---|---|
| P4-1 / #45 | PDF 实体页仍将组织标签扩写成实体属性；仅增加提示未完全修复 | 在原有事实→生成→核验入口，将原件标签/明确断言及模型释义的角色区分贯穿到候选与标题核对。用无品牌的小型受控数据覆盖组织标签、明确属性标题和同文不同上下文；不按固定词语屏蔽。对有歧义且不能支撑的候选记遗漏，独立知识正常发布 | 组织标签只保留原义，真正标题事实不丢；相关标题/摘要和正文均核对；同一 PDF/PPTX 只记回归 |
| P4-2 / #45 | DOCX 表头角色判断不稳定；PDF 追问两次核验输出截断；无完整有效核验不能作为成功答案 | 先在受控服务验证回答核验按有界单元处理的协议：保持原问题范围、必要行关系与证据绑定，单元结果不能跳过全回答所需覆盖；无效协议不授权扩大修改范围。区分首行原始文字、布局支持的表头判断与未验证角色，不靠全局忽略表头问题放行 | 原文必要条件和全部匹配行保留；引用/语义核验都有效；累计预算包括失败、分批与纠正，截断不能静默放行 |
| P4-3 / #45 | 六行表冷导入的核验消耗 85,789 tokens，PDF 核验 65,285；成本未通过，最终提示版也没有冷基线 | 按物理 payload 阶段对账，先处理重复证据/长身份元数据与核验重复输入，保留源身份和完整语义。优先复用已证明有效且契约一致的核验结果；有界核验不可通过删信息或取消检查省费。对同一冻结输入只做新版本的必要冷回归，旧开发账单独保留 | 能力、保留锚点和缺失集合相等后逐例核对累计 tokens/B0≤1.10；不可比项不计算通过比率；不宣布未经成对观测的速度收益 |
| P4-4 / #34 | 历史 B1 无当前零知识/最终回答核验能力；PDF OCR 资产未采纳，XLSX 发布缺多行，不能称完整同能力对照 | 保留 `c04d8be` 及本轮原始结果；先按入口行为、索引消费、原文锚点、遗漏集合建立等价矩阵，仅将满足矩阵的范围列为可比。完整能力若无历史等价点，应另立清楚标识的对照版本并记录其相对历史 B1 的差异，不能偷偷补改或重命名旧 B1 | 报告区分历史 B1、受限组及真正同能力对照；原 B0 不替换，旧成本不重置 |
| P4-5 / #45 | PDF/PPTX 的失败都已经参与调参，后续结果不是独立验证 | 先完成上述离线修复，再按修订后的冻结验证清单安排语义不同的小文件独立验证；当前两份继续作为回归。不得换文件抹去失败，也不自动扩大真实实验刷通过 | 冻结实现后的独立结果满足语义/引用门槛，失败仍保留；原有样本成本与结果可追溯 |
| 最后 / #23 | #34/#45 仍开放，语义、等价性和成本条件未齐 | 在子项满足后逐条核对总规格，继续使用小文件最小闭环 | 不以正常发布、单个正确答案或代码已提交替代总规格验收 |

P4 是剩余修复计划，不是本轮已经实现的声明。普通内容错误继续按遗漏处理；知识为 0 时
也正常登记发布，用户后续编辑。硬额度、用户停止、存储/身份/事务和真实人工覆盖保护仍保留。
这些未满足项不要求重新全量导入旧 23 份大文档。

收尾：本轮 518 次物理模型请求、3,896,228 tokens，未知用量 0；云 OCR 10 页次/28 HTTP。
最终全量 2,181 passed、1 skipped，桌面 I/O 独立 1 passed；mypy 311 文件和修改文件 Ruff 通过。
代码及报告的 Standards/Spec 复核无剩余可操作问题；真实语义失败仍按上表保留。
全部 issue 已读回：43 个关闭，#34/#45/#23 三个开放。代码与报告提交在本地，未推送。

## 2026-09-14 第三轮交付与下一步

授权上限仍按用户本次明确给出的 8,000 万 tokens、2,000 请求、5 小时、100 页次 OCR；
本轮单独起点为 05:32:30 UTC、截止 10:32:30 UTC。实际 67 操作已结算为 442 请求、
4,320,146 tokens、未知 0，云 OCR 10 页次/170 HTTP。本轮派发已封存，旧失败未清零。

已提交 `5673104`、`0ed90e4`、`26f492e`、`b1b1250`、`998b561`：

- P4-1：生成以原文引句为依据，提取器释义不再作为额外事实权威；原文/阅读器角色
  贯穿编译及问答，两种事实复用绑定实际角色契约。受控错误候选可排除且正常零知识发布。
- P4-2：回答按有界单元核验，每批保留整题、全文和全部观察；截断严格减小批次，
  单单元截断不放行。完整元数据数组/对象严格按原路径与类型校验，无效协议仅一次反馈。
  带 `@` 的精确证据标记已贯通引用渲染；未知标记仍拒绝，真实 DOCX 引用回归完成。
- P4-3：核验中的重复长身份采用可逆编码，离线真实请求投影约减 20%–36% 输入 tokens；
  没有把投影当作实际全流程节省。最终小表冷导入 140,842 tokens，全部候选排除后
  正常发布；六行原文仍可读，首问/追问完成，但知识覆盖与对照不同，不能用于同覆盖成本收益结论。
- P4-4：历史 B1 保留，新增明确命名的 B1-R3 候选对照 `26f492e`。五格式冷导入均
  正常发布，失败和缺失集合逐例记录；其能力/知识集合未完全等价，#34 仍不能关闭。
- P4-5：两份语义不同的一页小文件在调用前冻结。DOCX 暴露编译来源归属问题后转为
  回归；扫描 PDF 经队列拒绝、pending 后沿原作业成功恢复 OCR，首问完成，追问因
  附加句数/唯一性表述被拒绝。两者均未形成完整独立通过；没有换样本抹掉失败。

全量 2,213 passed、1 skipped，桌面 I/O 独立 1 passed；mypy 312 文件及修改文件 Ruff
通过。生产增量双向复核无剩余问题。原件、旧答案和失败证据留在验证库，未手改知识
提高自动质量成绩。普通内容异常不影响来源及可靠部分正常发布，后续人工编辑仍允许。

| 顺序 / Issue | 尚未满足的具体原因 | 下一步修复与最小验收 |
|---|---|---|
| P5-1 / #45 | 原文和阅读器注解仍共存于 context 字符串；部分知识页写入“未经来源确认”等处理说明，以及模型局部引用别名。仅传递角色说明没有消除全部误归属 | 先用已保存请求构造公开导入入口回归，将原文片段、结构关系和阅读器状态逐项分离并绑定缓存依赖；有效原文同名短语不能被关键词删除。生成只保留有原文支持的知识，定位/处理说明进入既有报告。无效候选排除、零知识发布及独立内容继续必须同时通过，再做同一小 DOCX 的一次必要回归 |
| P5-2 / #45 | 主要答案正确时，额外章节层级、拼接标题或句数/唯一性断言仍可能出错；核验已正确阻止完成，但尚无整组独立通过 | 先按原始回执复现每个多余断言的来源，核对问答输入是否把导航提示当成事实或丢失原文结构。仅对可证明的输入/协议缺陷修复，不放宽核验或无限增加语义重试；受控用例保留完整原问题与引用，真实复测继续使用现有文件并记为回归 |
| P5-3 / #34 | B1-R3 只是明确命名的候选对照；原 PDF 的 OCR 覆盖、小表知识集合和 PPTX 标签语义不相等，最终版也没有五例完整冷配对 | 以已存矩阵先排除不等价范围，核对原文锚点、知识锚点、遗漏、索引实际消费及发布/恢复能力。正常零知识发布可通过功能验收，但不能冒充同知识覆盖。仅对修复后满足同能力/同覆盖的范围安排冷配对；B0、历史 B1 与开发成本全部保留 |
| P5-4 / 成本列报（非关闭阻塞） | 维护者已明确取消成本关闭门槛；既有超限支出及未知用量仍是实际账目 | 保留完整账本并持续列报，不再等待口径确认，也不为降低历史累计追加付费实验。性能结论仍需有效可比证据，执行硬额度不变 |
| 最后 / #23 | #34/#45 未达到全部条件 | 子项完成后逐条核对；正常发布、受控测试通过和局部正确答案都不替代总体验收。继续用小文件，不扩大到旧 23 份大文档重跑 |

本轮新增关闭 0 项，#13/#25/#46 保持关闭；开放仍为 #34/#45/#23。
以上是剩余修复计划，不是已经通过的声明。详细运行与费用见第三轮报告。


## 2026-09-14 P5 离线开发进展

P5-1 的 DOCX 切片已完成通用实现：原文摘录/结构/阅读器状态分离、生成身份标记不得进入正文、相关缓存契约更新，旧原文及历史引用保留。审查发现的列表层级类型、异常跨度、程序占位说明及跨段数值拼接均已修复；普通结构异常作为局部缺失，不阻断正常发布。新增输入说明仅按实际字段追加，私有标记使用较短编码；合成小上下文测试仍保留硬限，并验证同身份 Continue 复用。XLSX/PDF 等格式没有宣称已完成结构化上下文升级。

本次只使用小型受控文档；付费模型及 OCR 新支出为 0，r3 已封存额度未重开。184 个历史引用按当前代码完整回读一致。Standards/Spec 二次修复后的复核均无剩余问题；完整测试结果见 [P5 离线记录](import-p5-offline-validation.md)。

剩余顺序：

1. P5-2：已定位原始核验/修正回执，区分额外拼接标题、格式与语义角色混淆、句数/唯一性新断言；先补公开问答入口的反例，不把需要语义判断的问题简化为关键词规则。
2. P5-1/P5-2 的真实回归：有效新实验额度确定后，现有小 DOCX 只做一次必要回归；继续使用现有失败文档，结果按回归报告。其他原生格式按有证据的缺口安排小切片，不扩大大文档全量重跑。
3. #45：实现冻结后才按独立小文件清单验证；当前受控通过不能代替完整独立语义结果。
4. #34：只有能力、知识覆盖、遗漏及质量一致的范围才安排冷配对；费用全部列报，不因费用高而拒绝关闭，也不凭不等价结果宣称提速。
5. #23：#34/#45 满足后再核对总规格；当前三票保持开放，#13/#25/#46 保持关闭。

## 2026-09-15 第四轮交付与下一步

本轮授权 8,000 万 tokens、2,000 请求、6 小时、100 页次 OCR；独立账本起点
2026-09-14 21:30:02 UTC，截止 2026-09-15 03:30:02 UTC。先前轮次不重开；最终费用与
测试以 [第四轮报告](import-p5-live-r4-validation.md) 为准，费用不阻塞 issue 关闭。

已交付 `681a911`、`0f26127`、`02dd39e`、`b558f2e`、`42c7d8b`、`bd469a4`、
`18963bc`、`3df1f09`、`ad27d8b`、`af0cd1b`：

- DOCX v13 将程序缺失占位说明与原文分开，保留断词边界及原文同名描述；缺图确认绑定
  原件、位置和数量，表格预览不重复 OCR/诊断；旧解析及184条历史引用不原地改写。
- 核验数字按 JSON 十进制字面值精确比较，保留原本可引用的 JSON 文本。错误路径只给
  首个失败位置和有界真实键，不猜路径、不放行无效证据、不增加重试次数。
- 生成器自述和页面导览不得混入知识；翻译保留术语范围和条件作用，中性标题仍允许。
  原图按精确资产绑定，忠实描述不必逐字复制 alt；裁剪范围和未见视觉细节仍不得虚构。
- 旧两份失败文件及五格式继续作为回归；小文本最终三条知识正确，新扫描 PDF 首次
  零知识也正常发布，修复后同身份 Continue 正常产生可靠知识。所有失败与费用保留。
- 两份新小文件完成首次独立调用，但完整质量未通过：PPTX 页脚/标题/占位符角色混淆，
  PDF 过强图片不确定性说明；主事实正确不能抵消这些附加断言。PDF 后续修复仅算回归。
- 原图窄回归又定位到 OCR 自动图标签被当作原作者对原图的指定。v14 已从解析步骤
  保存原始字节、完整显示帧和 OCR 输出的精确关系；原文 alt 单列、关系限于本块资产、
  完整元数据沿原文上下文分页，旧解析不猜测补写。两个 PNG 编码不同但像素一致，
  不能将其中一个称为预览误选；OCR 裁剪不继承完整图范围。此项是 P6-2 的已完成切片，
  尚不代表所有图片及附加说明问题解决。
- v14 同一原图问题已完成并保存，选中原始 PNG，仍有多余处理说明；普通复导零模型/
  零 OCR。当时DOCX整个表格主题被排除；`18963bc` Continue已恢复A行及B行排他条件，
  该版知识不含图片，图片原件/资产仍可读。ad27d8b契约升级后重编译为三页，表格及断电前提完整、图片事实有遗漏；覆盖逐版报告，不能只择最好一版。
- P6-1：PPTX按真实OOXML类型识别原生标题，记录类型、标题数量及唯一对象，不按idx=0、
  对象名或顶端位置猜测。`ad27d8b`继续区分原生标记与有原文支持的语义标题/表头。
  Word与PPTX的Continue均正常发布；18963bc两个首问失败保留。ad27d8b同首问/追问均完成，仍有相关邻接图文及卡片指代的过强不确定性，未宣称独立全通过。
- P6-2：去掉视觉工具禁用提示中鼓励无关能力说明的冲突。真实原图选择正确，但仍附加
  说明；核验总判定与问题列表矛盾又导致InvalidReview。`ad27d8b`已提供具体有界诊断，
  不自动改判、不增加一次协议恢复上限。公开会话反例先RED后GREEN，113项组合通过。

- `af0cd1b`：完整测试暴露PPTX/图像规则无条件附给其他格式，导致4K上下文过度分批、
  表格遗漏及共享用例耗尽预算。现按实际证据附带说明，DOCX附件内的PPTX沿原生位置链
  保留角色，缓存绑定实现版本；未删原文、未提高预算或放宽核验。73项组合、mypy315及
  双轴审查通过；最终全量2,287 passed、1 skipped，桌面I/O独立1 passed，27文件Ruff通过。
  中间全量的数字碰巧命中资产哈希属于测试筛选错误，02b772b改用完整原句，原失败保留。
- 同一原图、Word表格/断电、PPTX活动/迟到与卡片问题在ad27d8b均完成；普通同配置复导
  零请求。两次从18963bc到ad27d8b的契约升级重编译另列40请求/242,057 tokens，不计
  零请求复导，也不冒充冷导入。仍有附加内容及过强指代未知说明。
- 分项标题/正文审核诊断拒绝误译负例，却误拒中性标签正例，2请求/14,840 tokens；
  该协议没有进入产品代码。随后按现有聚合协议、只追加语义条件与导语标签区分的说明，
  四项控制中三个正例通过，但原故障译义负例再次漏检；新增4请求/23,377 tokens，同样
  未上线。两种诊断分别失败，六项合计38,217 tokens全列报，不按单个成功改写质量结论。

本次结算105操作合计611次模型请求、5,998,090已知tokens、未知0；云OCR 3页次POST/19 HTTP。
前三轮加本轮为1,671请求、14,989,102已知tokens；总请求数内第一轮3次用量未知，金额未知。
184条旧引用再次回读一致，26份完成答案的机械引用检查无问题。费用只列报，不能以
省略更多知识制造性能收益。本轮仍按原始六小时时限，尚未耗尽的额度不被重置。

下一步按下表执行，普通发布功能不等待这些问答质量项通过。每项先使用已保存回执与
现有公开入口证明通用改动，不扩大到旧23份大文档，不手改知识制造通过。

| 顺序 / Issue | 未满足原因 | 通用修复与最小闭环 | 完成条件 |
|---|---|---|---|
| P6-1 / #45：结构角色切片已交付，语义关系继续 | 原生占位符/标题数量、语义表头及附件传播已修复；现PPTX回答仍过强质疑“其他声部”与上句绿卡的指代，旧night mode曾变成nightly，不能用原生角色字段解决全部语义问题 | 冻结已有原文、完整观察及对应候选，逐条区分位置证据、措辞和相邻指代；先检查query/answer_review的实际输入是否缺必要上下文或存在冲突规则。没有输入缺口则明确记语义失败，不新增猜测角色字段；保留带条件标题及歧义标题的成对回归 | 标题/段落中的前提、模式与作用对象保持；已知原生字段继续贯通普通/附件格式，不能靠取消相关核验换取通过 |
| P6-2 / #45：回答范围继续 | ad27d8b核心问答完成，禁用视觉说明已消失，但仍有邻接图文、技术字段及过强未知关系说明；旧表格/扫描案例的额外缺失断言保留 | 以已保存问答的完整问题和全部观察冻结正反例：明确请求图片能力时保留真实限制，仅展示原图时不加无关限制；原件明确“未说明”与模型仅未读到分开。只对实际冲突的生成/审核指令或证据绑定修复，程序不按关键词删句，保持既有一次协议恢复与限定语义修正 | 必要前提、六行和图注完整，额外断言有证据且与问题有关；无法解决的候选有界失败，资料仍正常发布 |
| P6-3 / #45：标题译义漏检继续 | 完整原文与标题核验规则已有，聚合核验仍漏掉故障译义；分项诊断拒绝负例却误拒中性正例，语义条件说明通过三个正例却仍漏掉负例，两种假设均未上线 | 六项冻结诊断/原回执保留。下一切片先在相同证据下验证可定位标题及正文单元的显式覆盖与各自判定，优先复用现有回答核验的有界单元/原文绑定做法；每个单元仍看到完整条件，不能靠截断邻接证据或宽泛引用自证。正反组同时通过后再改evidence_verifier协议，并从import_document验证无效审核有限恢复、候选遗漏及独立内容发布 | 错误标题被拒绝且忠实条件/中性标签/保留原词允许；未评单元不冒充完成，预算与原件保护不变。当前这一切片尚未实施，不声称语义问题已解决 |
| P6-4 / #34：等价对照仍缺 | 同配置文本冷对照的锚点覆盖相同但两边均有语义错误；Word表格/图片及PDF覆盖逐版变化，最终版无有效同质量冷组 | 保留B0/B1/B1-R3，先按保存矩阵逐项列原文、知识、遗漏、索引消费、发布/恢复能力和语义结果。只在同能力/覆盖/质量范围安排一次必要冷配对；跨契约重编译、Continue与稳定复导分账。无稳定收益就报告无性能结论 | 有效可比范围和不可比原因可审计；费用无关闭门槛，不能缩减内容制造收益 |
| P6-5 / #45，最后 #23：独立验证与总规格 | 两份新独立样本都曾失败且参与开发，后续改善只属回归；#34/#45条件仍未齐 | 完成上述有证据的修改后固定提交、输入/角色/问题/锚点/预算。未来独立验证先冻结最小清单再调用，不自动替换失败样本或增加大文档。#45/#34实际满足后逐条复核#23，已完成实现票保持关闭 | 完整质量及有效对照有真实结论；不以受控通过、正常发布或单个正确答案替代总验收，不因内容缺失中断发布 |

当前46票中43票已关闭，本轮未新增可关闭项；#13/#25/#46维持关闭。
这些是具体剩余任务，不是已完成声明，费用仍只列报。
