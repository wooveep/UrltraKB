# P5 离线修复记录（2026-09-14）

本次承接 #45 的 P5-1：分离 DOCX 表格的原文、结构和阅读器状态，阻止生成请求中的临时事实引用 ID 进入公开知识正文。修复应用于通用解析、模型输入与输出协议，没有按品牌、文件名或固定答案调整验证库知识。

## 行为与边界

- 生成请求为事实及原文身份选择不出现在输入内容中的私有标记。标记只能留在结构化身份字段；进入标题、小标题或正文的候选走既有有限恢复及内容遗漏路径。即使语义审核支持，违规候选也不能直接发布。
- 真实原文中的 `r8`、`@r:1` 等与私有标记形似的文字保持原样。此次机械检查针对生成请求的身份别名，不宣称能识别模型擅自改写的别名或所有无依据断言。
- DOCX 新解析记录增加可选 `context_data`：原始首行/声明表头单元格摘录、结构关系、阅读器状态分别存储。原文节点遍历保留段落及注释边界，不执行 OCR，不把缺失脚注、评论、图片或附件的程序占位说明放进原文摘录；可读脚注/尾注/评论/图片替代文字保留其原始角色。`first_row` 表示位置，不表示已经确认的表头。原文中的同名状态短语仍是原文。
- 提取、生成、标题上下文、核验、遗漏依赖检查及导航使用同一上下文投影；编译通用入口只为实际携带结构化上下文的请求追加对应说明及字段角色，已带相同说明的请求不重复追加；问答读取将结构化上下文分页传递，标注格式和完整性。完整对象不能绕过窗口上限重复返回。
- 既有显示 `context` 保留。旧解析记录缺少新字段时，块和解析 ID、原文文本、坐标、引用继续有效，不修改已有解析产物。DOCX 新解析配置为 v12；后续导入/Continue 会按新配置重新匹配解析缓存并保存新的解析版本，也可通过显式重新解析入口更新；已有解析产物和旧引用不原地改写。本次未在验证库自动重新导入这些历史来源。
- DOCX 附件转交保留新字段以及原始附件坐标；并更新解析、生成、共享分析及导航缓存依赖。新契约不沿用旧生成结果。
- 普通内容异常继续作为缺失处理，可靠内容或零知识来源入口正常发布；用户仍可手动补写。取消、硬额度、身份/存储/事务以及人工内容保护不变。

此切片只为原生 DOCX 表格生成结构化上下文；XLSX/PDF 等旧格式仍按既有混合上下文契约读取，不能称全部格式已完成字段分离。

## 验证

公开入口的受控模型回归已复现：私有引用泄漏、原文/表格状态混合，以及附件转交丢失结构化上下文。逐项修复后验证正常及零知识发布、原文字面保留、历史格式加载、长表头分页、问答引用和附件位置。测试使用小型生成文档和本地 HTTP 服务；不把受控审核通过当成真实模型语义通过。

中断前完成 93 项定向回归；恢复后重新执行阅读器相关回归 22 项全部通过。恢复后扩大定向回归 115 项通过。双向审查发现并修复三项：正常列表层级的字符串转换、非法跨列值导致整份解析失败、原文摘录夹带缺失脚注的程序说明。新增公开入口回归先复现三个失败，再进行修复；非法结构只记局部缺失，真实文字不按关键词删除。二次审查另发现多段落文字可能拼接成新数值，已按段落、脚注/评论、图片及附件边界分开摘录，并先记录两个失败回归再修复。两次完整回归均因审查新发现主动中止，不能算完整通过；各自日志保留。之后完成的一轮全量为 9 failed、2,223 passed、1 skipped：一项仍断言旧 context 字段，八项触及合成测试的 100 请求硬限。前者改为精确核对原始表头摘录及坐标；后者定位出无条件携带新说明和私有 ID 编码膨胀。已让新增说明按实际数据出现，且把私有标记从 Unicode 改为 ASCII：单个示例在 o200k 分词器由 9 降为 4 tokens，这不是整条调用或真实导入成本结论。共享请求四项恢复通过；160 段并发仍存在新增协议开销，保留原样本及每轮 100 次硬限，只允许测试通过同一公开 Continue 入口恢复一次，并验证首次精确达到硬限、未发布未完成知识、事实不重复及检查点完整。完整并发测试 8 项通过；未增大生产额度或语义重试次数。最终完整回归为 **2,233 passed、1 skipped**（672.31 秒），桌面 I/O 独立 **1 passed**；mypy **314** 个源文件、30 个修改 Python 文件的 Ruff 检查及格式检查通过；Standards/Spec 最终均为 **0 项剩余问题**。跳过项为未配置可选 `OCR_RUNTIME_PYTHON` 的本地 OCR 计费测试，没有用本地运行替代云 OCR 验证。全量有三条 LiteLLM 日志协程 RuntimeWarning，原样保留在测试日志中。

当前代码重新读取 r3 的 184 个历史引用，文本、上下文、坐标、类型、资产、游标及引用字段与旧 JSON 记录完全一致。初版检查脚本曾把 Python tuple 与 JSON array 直接比较造成资产字段假差异，按同一 JSON 序列化口径修正后差异为 0；两次检查记录均保留。

一次启动脚本未在前置格式检查失败后退出，误启动的 pytest 覆盖了上述 9 项失败全量运行的原始日志。其已返回的结果摘要及独立失败复现日志保留；摘要不冒充原始完整日志。误启动的运行已主动中止并另存记录；最终运行改为前置检查成功后单独启动。

完整测试覆盖当前工作区，其中含未纳入本次提交的既有桌面相关修改；结果不等于单独 Git 提交快照已经在洁净环境验证。

持久验证目录：`examples/kb/Ocloudware/outputs/issue45-p5-offline-2026-09-14/`。中断前 `/tmp` 日志未保留，不能宣称那些原始日志已入库；恢复后的日志保存在该验证目录。

## 成本与未完成项

本次新增付费模型请求 0、付费模型 tokens 0、云 OCR 请求及页次 0。上一轮 r3 在 2026-09-14 10:32:30 UTC 截止且已封存，本次没有重开该额度。受控 HTTP 返回的合成 usage 不是供应商账单。

历史账本全部保留：r3 为 442 次模型请求、4,320,146 tokens、10 OCR 页次/170 OCR HTTP；三轮小文件账目合计 1,060 次模型请求、8,991,012 已知 tokens，以及第一轮 3 次未知用量请求。金额未取得，仍为未知。成本完整列报，不再作为 issue 关闭门槛；实际执行硬额度仍有效。

#45 尚缺已有小 DOCX 的真实回归、额外回答断言的 P5-2 修复证据及完整独立语义通过；#34 尚缺同能力、同覆盖、同质量的有效配对；#23 依赖两者。新增关闭 0 项，#13/#25/#46 保持关闭。

下一步依次为：按保存的请求复现回答附加断言；修复可证明的通用输入/协议缺陷；获得有效新额度后对同一小 DOCX 做一次必要回归，随后按冻结清单验证独立小文件；能力和覆盖等价的范围才进入冷运行配对。旧失败、旧 B0/B1 及所有支出不重置，不以换文件冲掉失败。


## P5-2 的原始回执定位（只读，不改判既有失败）

已核对 r3 的原始请求及响应：DOCX 的 00416/00419 分别拒绝额外层级说明及把两个段落拼成带斜杠的单一栏目标题；扫描 PDF 的 00433 拒绝引用位置和 OCR 不可用断言，经 00435 修正后，00438 又拒绝新增的句数/唯一性表述。00441/00442 局部支持不能抵消整题仍有拒绝单元。

上述失败核验（00419/00438）中，已读取范围的 next 均为空，各返回块的 context_complete 均为 true；这证明所取范围完整，不证明未取范围没有其他内容。扫描案例中两句原文均在观察内，不能把该错误归因于缺少第二句或必须重做 OCR。DOCX 早期核验理由把原生 paragraph 格式用于否定标题语义，仍需区分“解析结构”和“实际语义角色”；这不自动证明后续拼接标题正确，也不能据此整体放宽核验。

后续修复应分别验证：段落也可以承担真实标题语义；导航预览不能授权合并新标题或增加层级；回答修正不能新增未经原文支持的句数/唯一性说明。先在公开问答/修正入口复现明确的协议缺陷，再选择通用修复；没有可证明的缺陷时，不靠无限重试或样本特判强行变成通过。
