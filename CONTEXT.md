# UrltraKB

UrltraKB 将原始资料积累为相互关联的知识，并支持围绕这些知识进行问答、对话和产物生成。

## Language

**知识库（Knowledge Base，KB）**：
一组共同管理的原始资料、编译后的知识及相关对话与产物。用户可以拥有多个相互独立的知识库。
_Avoid_：将整个应用或所有知识库的集合称为“知识库”。

**当前知识库（Active Knowledge Base）**：
用户当前选定用于浏览或发起操作的知识库。切换当前知识库不代表合并或迁移其知识内容。
_Avoid_：当前项目、当前资料库。

**知识编译（Knowledge Compilation）**：
将原始资料整理为摘要、概念、实体及其关联，并结合既有知识持续更新的过程。
_Avoid_：将知识编译等同于文件格式转换或单次问答。

**资料（Source Document）**：
来自文档或网页、作为知识编译依据的输入内容。一份资料可用于形成多个知识页面；不同来源即使内容相同，仍保留各自的资料身份。
_Avoid_：将编译后的知识页面或生成产物统称为“资料”；仅因内容相同就合并不同来源的身份。

**资料接入（Source Intake）**：
将原始资料保存并纳入知识库管理的过程。资料接入完成不表示知识编译已完成，尚未编译完成的资料仍可保留并继续处理。
_Avoid_：把“原文已保存”和“知识编译完成”合称为同一种成功。

**资料版本（Source Version）**：
同一份资料在某一时点的内容。同一来源以相同内容再次接入不产生新版本。引用绑定其依据的资料版本，资料更新后，被引用的旧版本仍可读取。
_Avoid_：让旧引用自动指向最新内容；把重新解析等同于原始资料更新。

**解析版本（Parse Version）**：
一份资料版本所对应的一组具有独立身份的解析结果，包含内容片段、来源位置及相关资产。同一资料版本可以对应多个解析版本，既有证据仍指向其依据的解析结果。
_Avoid_：把更换 OCR 模型或重新解析当作原文更新；用新解析结果覆盖旧证据所依据的内容。

**解析复核（Parse Review）**：
对照原始资料核对转写和内容结构，修正错字、错列或遗漏等解析问题的过程。修正保留原图或原文位置与处理出处；已留存结果被修正时形成新的解析版本。
_Avoid_：把图片的业务含义推断当作转写修正；把未核实的模型改写当作逐字原文。

**资料证据（Source Evidence）**：
用于支持知识结论的原始资料片段，可以追溯到所依据的资料版本和具体位置。
_Avoid_：把目录摘要当作原文细节的完整依据；给无法确认出处的内容标注貌似精确的位置。

**本地 OCR（Local OCR）**：
在用户设备上识别资料图像内容的方式，包括系统 OCR 和飞桨 OCR。
_Avoid_：将“本地 OCR”专指飞桨模型；把识别引擎与其使用的 CPU/GPU 混称。

**系统 OCR（System OCR）**：
由操作系统提供、供资料识别使用的 OCR 能力。
_Avoid_：将应用另行安装的飞桨模型称为系统自带 OCR。

**飞桨 OCR（PaddleOCR）**：
用户选择的、在本机运行飞桨文档识别模型的资料识别方式。
_Avoid_：与系统 OCR、知识编译模型或远端飞桨服务混称。

**云端 OCR（Cloud OCR）**：
由用户选定的远端识别服务处理资料内容的方式。
_Avoid_：将下载本地模型文件称为云端 OCR。

**图片理解（Image Understanding）**：
围绕资料中的图片解释图表、关系或视觉内容的能力，形成可追溯至原图的视觉观察。
_Avoid_：将图片理解等同于 OCR 逐字识别或知识编译。

**视觉观察（Visual Observation）**：
图片理解产生的、与原图及分析问题关联的文字描述或判断。
_Avoid_：把模型对图片的推断当作原生正文或逐字引用。

**知识页面（Knowledge Page）**：
知识库中可独立浏览和引用的知识内容单元，可以关联一份或多份来源资料。
_Avoid_：将知识页面等同于原始文档或应用的界面页面。

**知识主题（Knowledge Topic）**：
围绕稳定功能、任务或知识对象组织的内容范围，可以包含多个事实及其前提、步骤、参数和例外。部署类资料优先围绕功能或部署任务归并主题，独立实体仍保留自己的知识页面。
_Avoid_：把每个参数或事实都当作独立主题；把原文章节切分直接等同于知识主题划分。

**知识库任务（Knowledge Base Task）**：
针对一个知识库发起、具有明确完成或停止结果的一次操作，例如一批资料导入或一次知识编译。
_Avoid_：将持续的目录监听与一次知识库任务混称。

**资料处理项（Document Processing Item）**：
知识库任务中围绕一份原始资料执行的一次完整处理。批量资料任务由多个资料处理项组成。
_Avoid_：将整批资料或处理过程中的单个阶段称为一个资料处理项。

**目录监听（Directory Watching）**：
持续关注某个知识库的原始资料目录，以发现新资料并触发处理的活动。
_Avoid_：将目录监听等同于一次目录导入。

**生成产物（Generated Artifact）**：
由问答、聊天、检查或生成流程保存的内容成果，例如检查报告、Skill、幻灯片或知识图谱。产物存在并不表示相关任务或质量校验全部成功。
_Avoid_：把过程输出、未保存的预览或校验通过直接等同于产物。

**对话会话（Chat Session）**：
属于一个知识库、共享连续对话上下文的一组问答回合。不同入口可以打开同一会话；打开它的窗口不等于会话本身。
_Avoid_：将会话等同于桌面窗口或单次模型请求。

**完整回合（Completed Turn）**：
会话中一次用户输入与成功完成、已保存的回答。中断时的部分回答可以展示和复制，但不构成完整回合。
_Avoid_：将流式片段、工具产生的独立产物或未完成回答称为已保存回合。


**内容遗漏（Content Omission）**：
原始资料中因无法可靠处理或核验而未进入本次知识成果的内容范围。遗漏应当明确记录并允许补全；已发布内容可用不代表原始资料已被完整覆盖。
_Avoid_：把遗漏当作原文没有相关信息；把未通过核验的内容当作已确认知识。
